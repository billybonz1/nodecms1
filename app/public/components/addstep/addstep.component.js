import template from './addstep.html';
import controller from './addstep.controller';

export const AddStepComponent = {
  restrict: 'E',
  scope: {},
  template,
  controller,
  controllerAs: 'addstep',
  bindToController: true
};
